import 'package:app/export.dart';

class LoadingCircularView extends StatelessWidget {
  const LoadingCircularView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
